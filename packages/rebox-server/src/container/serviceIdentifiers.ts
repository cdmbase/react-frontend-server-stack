// server bootstrapper
export const SERVER_BOOTSTRAPPER = Symbol('ServerBootstrapper');

// config stores
export const SERVER_CONFIG_STORE = Symbold('ServerConfigStore');

// middlewares
export const MIDDLEWARE_FACTORY = Symbold('MiddlewareFactory');
